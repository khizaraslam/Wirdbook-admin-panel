export enum siteRoutes {
  login = "/auth/login",
  home = "/",
  dashboard = "/dashboard",
  tabs = "/tabs",
  lectures = "/lectures",
  qa = "/qa",
  banners = "/banners",
  hijriEvents = "/hijri-events",
  syncModules = "/sync-modules",
}
